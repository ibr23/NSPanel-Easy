Program.s
    //The following code is only run once when power on, and is generally used for global variable definition and power on initialization data
    int sys0=0,sys1=0,sys2=0     //At present, the definition of global variable only supports 4-byte signed integer (int), and other types of global quantity declaration are not supported. If you want to use string type, you can use variable control in the page to implement
    // Globals
    int api=0
    int theme=1
    // Colors
    int color_page_background=61277
    int color_containers=65535
    int color_text=21130
    int color_button=38066
    int color_button_text=61277
    int color_button_pressed=1276
    int color_button_pressed_text=65535
    // Components
    int home_weather_pic=0
    // Model & EEPROM management
    int hard_coded_display_mode=1  // 1=EU, 2=US, 3=US Land, 4=Blank, 5=Easy EU, 6=Easy US, 7=Easy US Land
    int display_mode=0
    int magic_flag_found_at_boot=0
    repo sys0,0  // Read magic byte from addresses 0-3
    if(sys0==2037602629)  // "Easy" in ASCII little-endian
    {
      magic_flag_found_at_boot=1
      repo display_mode,4  // Read display_mode from addresses 4-7
      if(display_mode<1||display_mode>7)
      {
        display_mode=hard_coded_display_mode
        wepo hard_coded_display_mode,4
      }
    }else
    {
      display_mode=hard_coded_display_mode
      wepo 2037602629,0   // "Easy" magic
      wepo hard_coded_display_mode,4
      bauds=921600 //Configure baudrate
    }
    // General settings
    baud=921600//Configure baudrate
    dim=100//Configure backlight
    recmod=0//Serial data parsing mode:0-Passive mode;1-Active mode
    printh 00 00 00 ff ff ff 88 ff ff ff//Output power on information to serial port
    page 0                       //Power on start page 0
